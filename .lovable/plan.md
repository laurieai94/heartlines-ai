

## Update tagline text in HeroCarousel

**File**: `src/components/HeroCarousel.tsx`

**Change**: Replace all 10 tagline values from `"this is what it's all for"` to `"what it's all for"` (lines 24–33).

Single find-and-replace across the slides array — no other files affected.

