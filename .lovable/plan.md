

## Update "Why heartlines" section in README.md

Replace the current "Why heartlines" section (heading + body) with the user's new copy, changing the heading to "Why I Built This" and using their exact text.

### Changes
- **`README.md`** — Replace the `## Why heartlines` heading and its two paragraphs with `## Why I Built This` followed by the user's provided copy verbatim.

