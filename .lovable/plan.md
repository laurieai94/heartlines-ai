

## Lowercase "heartlines" throughout README.md

Every instance of capitalized "Heartlines" in the README needs to be lowercased to match the brand's actual style (lowercase everywhere, as seen in `branding.ts`, `index.html`, and all UI components).

### Changes
- **`README.md`** — Replace all ~20 instances of "Heartlines" with "heartlines" throughout the file. The title becomes `# heartlines ai`. Alt text for the demo image becomes `heartlines ai`. All body copy uses lowercase.

### No other files affected
The rest of the codebase already uses lowercase consistently.

